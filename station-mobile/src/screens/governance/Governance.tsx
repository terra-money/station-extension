import React, { ReactElement } from 'react';
import { View } from 'react-native';

const GovernanceScreen = (): ReactElement => {
    return <View></View>;
};

export default React.memo(GovernanceScreen);
