import {
  Meta,
  Canvas,
  Source,
} from "@storybook/blocks";
import { RequiredText } from "../../../DocsHelpers.tsx";
import * as TooltipStories from "./Tooltip.stories.tsx";
import "scss/storybookoverrides.scss";

<Meta title="Components/Displays/Tooltip" />

# Tooltip

##### A Tooltip/Popover build on "@tippyjs/react".

## Props
  > `import { TippyProps } from "@tippyjs/react";`

## Import
```jsx
import { PageTabs } from "station-ui";
```

## Stories

## Default
<div className="story_with_source_below">
  <Canvas of={TooltipStories.Default} />
  <Source code={`
    <Tooltip content={<div>Tooltip</div>}>
  <div>Hover me</div>
</Tooltip>
  `} />
</div>

## Chains Box Example
<div className="story_with_source_below">
  <Canvas of={TooltipStories.ChainsBoxExample} />
  <Source code={`
    const chains = [{name: "terra",  img: "https://station-assets.terra.dev/img/chains/Terra.svg", balance: '100' }, { balance: '320' , name: "axelar", img: "https://station-assets.terra.dev/img/chains/Axelar.svg"}];

return (
  <div style={{ height: "100px", marginLeft: "50px", display: "flex", alignItems: "flex-end" }}>
    <Tooltip
      className={styles.tooltip}
      placement='top'
      content={
        <div className={styles.chains__list}>
          {chains.map((c, index) => (
            <div key={index} className={styles.container}>
              <img src={c.img} alt={c.name} />
              <div className={styles.text__container}>
                <span className={styles.chain}>{c.name}</span>
                <span className={styles.balance}>{c.balance}</span>
              </div>
            </div>
          ))}
        </div>
      }
    >
      <span className={styles.chain__details}>{chains.length}</span>
    </Tooltip>
  </div>
);
  `} />
</div>

<br />
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<br />
