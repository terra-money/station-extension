import { ReactNode } from 'react';
export interface DelegationCardProps {
    className?: string;
    validator: {
        name: string;
        img: string;
        description: string;
    };
    onClick: () => void;
    children?: ReactNode;
}
declare const DelegationCard: ({ validator, onClick, className, children, }: DelegationCardProps) => import("react/jsx-runtime").JSX.Element;
export default DelegationCard;
