export interface WalletSummaryCardProps {
    className?: string;
    walletType: string;
    walletAddress: string;
    walletBalance: string;
    prevTransactions: string;
    isActive?: boolean;
    onClick?: () => void;
}
declare const WalletSummaryCard: ({ className, walletType, walletAddress, walletBalance, prevTransactions, isActive, onClick, }: WalletSummaryCardProps) => import("react/jsx-runtime").JSX.Element;
export default WalletSummaryCard;
