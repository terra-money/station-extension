type ProgressBarData = {
    type: 'yes' | 'abstain' | 'no' | 'noWithVeto' | 'deposit';
    percent: string;
};
export interface ProposalCardProps {
    className?: string;
    proposal: {
        metaText: string;
        metaImage: string;
        title: string;
        submittedDate: string;
    };
    progressData: ProgressBarData[];
    threshold: number;
    progressLabelOverride?: string;
}
declare const ProposalCard: ({ className, proposal, progressData, threshold, progressLabelOverride, }: ProposalCardProps) => import("react/jsx-runtime").JSX.Element;
export default ProposalCard;
