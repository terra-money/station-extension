import { ReactNode } from 'react';
export interface VestingCardProps {
    className?: string;
    vestedAmount: string;
    children?: ReactNode;
    onClick?: () => void;
}
declare const VestingCard: ({ className, vestedAmount, children, onClick, }: VestingCardProps) => import("react/jsx-runtime").JSX.Element;
export default VestingCard;
