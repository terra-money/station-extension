import { ReactNode } from 'react';
export interface AssetSelectorToProps {
    extra: ReactNode;
    symbol: string;
    tokenIcon: string;
    onSymbolClick: () => void;
    chainIcon: string;
    chainName: string;
    amount: string;
    currencyAmount: string;
}
declare const AssetSelectorTo: ({ extra, symbol, tokenIcon, onSymbolClick, chainIcon, chainName, currencyAmount, amount }: AssetSelectorToProps) => import("react/jsx-runtime").JSX.Element;
export default AssetSelectorTo;
