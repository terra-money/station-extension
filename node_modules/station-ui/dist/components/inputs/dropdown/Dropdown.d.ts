export interface DropdownProps {
    options: {
        value: string;
        label: string;
        image?: string;
    }[];
    onChange: (value: string) => void;
    value: string;
}
declare const Dropdown: ({ options, onChange, value, }: DropdownProps) => import("react/jsx-runtime").JSX.Element;
export default Dropdown;
