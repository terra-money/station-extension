import { ModalProps } from "./Modal";
import { ReactNode } from "react";
interface ModalContextValue {
    openModal: () => void;
    closeModal: () => void;
}
export declare const useModal: () => ModalContextValue;
export interface ModalButtonProps extends Omit<ModalProps, "isOpen"> {
    renderButton: (openModal: () => void) => ReactNode;
    minimal?: boolean;
    children?: ReactNode;
    isOpen?: boolean;
}
declare const ModalButton: (props: ModalButtonProps) => import("react/jsx-runtime").JSX.Element;
export default ModalButton;
