import { PropsWithChildren } from 'react';
export interface MultiInputWrapperProps {
    label: React.ReactNode;
    layout: 'horizontal' | 'vertical';
    error?: string;
    warning?: string;
}
declare const MultiInputWrapper: ({ label, layout, error, warning, children }: PropsWithChildren<MultiInputWrapperProps>) => import("react/jsx-runtime").JSX.Element;
export default MultiInputWrapper;
