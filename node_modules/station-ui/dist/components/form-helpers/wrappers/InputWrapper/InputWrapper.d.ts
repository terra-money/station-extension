import { PropsWithChildren, ReactNode } from 'react';
export interface InputWrapperProps {
    label?: ReactNode;
    extra?: ReactNode;
    error?: string;
    warning?: string;
}
declare const InputWrapper: (props: PropsWithChildren<InputWrapperProps>) => import("react/jsx-runtime").JSX.Element;
export default InputWrapper;
