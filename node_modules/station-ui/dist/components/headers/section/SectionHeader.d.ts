import { ReactNode } from 'react';
export interface SectionHeaderProps {
    title?: string;
    extra?: ReactNode;
    withFilter?: boolean;
    withLine?: boolean;
    indented?: boolean;
    icon?: ReactNode;
    className?: string;
    onClick?: () => void;
}
declare const SectionHeader: ({ title, extra, withLine, indented, icon, className, onClick, }: SectionHeaderProps) => import("react/jsx-runtime").JSX.Element;
export default SectionHeader;
