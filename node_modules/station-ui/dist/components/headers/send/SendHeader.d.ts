export interface SendHeaderProps {
    heading: string;
    label: string;
    subLabel: string;
}
declare const SendHeader: ({ heading, label, subLabel }: SendHeaderProps) => import("react/jsx-runtime").JSX.Element;
export default SendHeader;
