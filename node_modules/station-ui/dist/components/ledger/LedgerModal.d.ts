import { PropsWithChildren } from "react";
export declare enum LedgerDeviceModel {
    NANOX = "nanox",
    NANOS = "nanos",
    NANOSP = "nanosp"
}
export declare enum LedgerDeviceAction {
    CONNECT = "connect",
    OPEN_APP = "openApp",
    CONFIRM = "confirm",
    UNLOCK = "unlock"
}
export interface LedgerModalProps {
    device?: LedgerDeviceModel;
    action: LedgerDeviceAction;
    appName: string;
    onRequestClose?: () => void;
}
interface Props extends LedgerModalProps {
}
declare const LedgerModal: (props: PropsWithChildren<Props>) => import("react/jsx-runtime").JSX.Element;
export default LedgerModal;
