export interface TabsProps {
    tabs: {
        key: string;
        label: string;
        onClick: () => void;
    }[];
    activeTabKey: string;
}
declare const Tabs: ({ tabs, activeTabKey }: TabsProps) => import("react/jsx-runtime").JSX.Element;
export default Tabs;
