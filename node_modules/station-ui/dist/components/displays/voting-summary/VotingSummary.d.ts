import { ReactNode } from 'react';
type ProgressBarData = {
    type: 'yes' | 'abstain' | 'no' | 'noWithVeto';
    percent: string;
    amount: string | ReactNode;
};
export type VotingSummaryProps = {
    data: ProgressBarData[];
    threshold: number;
};
declare const VotingSummary: ({ data, threshold }: VotingSummaryProps) => import("react/jsx-runtime").JSX.Element;
export default VotingSummary;
