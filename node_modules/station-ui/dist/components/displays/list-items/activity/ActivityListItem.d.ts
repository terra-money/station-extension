import { ReactNode } from 'react';
export interface ActivityListItemProps {
    variant: 'success' | 'failed' | 'loading';
    chain: {
        icon: string;
        label: string;
    };
    msg: ReactNode;
    type: string;
    time: string;
    timelineMessages?: ReactNode[];
}
declare const ActivityListItem: ({ variant, chain, msg, type, time, timelineMessages, }: ActivityListItemProps) => import("react/jsx-runtime").JSX.Element;
export default ActivityListItem;
