export interface AddressSelectableListItemProps {
    label: string;
    subLabel: string;
    active?: boolean;
    chain?: {
        icon: string;
        label: string;
    };
    onClick: () => void;
}
declare const AddressSelectableListItem: ({ label, subLabel, active, chain, onClick, }: AddressSelectableListItemProps) => import("react/jsx-runtime").JSX.Element;
export default AddressSelectableListItem;
