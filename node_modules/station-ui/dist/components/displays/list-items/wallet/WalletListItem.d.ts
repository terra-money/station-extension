export interface WalletListItemProps {
    name: string;
    address: string;
    emoji: string;
    walletBalance?: string;
}
declare const WalletListItem: ({ name, address, emoji, walletBalance }: WalletListItemProps) => import("react/jsx-runtime").JSX.Element;
export default WalletListItem;
