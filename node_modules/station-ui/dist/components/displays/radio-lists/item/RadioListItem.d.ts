import { InputHTMLAttributes } from 'react';
export interface RadioListItemProps extends InputHTMLAttributes<HTMLInputElement> {
    label: string;
    checked?: boolean;
    onClick: () => void;
    isOpen?: boolean;
    setOpenAcc?: any;
    accContent?: {
        name: string;
        icon: string;
    }[];
}
declare const RadioListItem: ({ label, checked, onClick, isOpen, setOpenAcc, accContent, }: RadioListItemProps) => import("react/jsx-runtime").JSX.Element;
export default RadioListItem;
