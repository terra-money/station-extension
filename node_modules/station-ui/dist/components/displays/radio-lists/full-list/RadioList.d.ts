import { ReactNode } from 'react';
export interface RadioListProps {
    children: ReactNode;
}
declare const RadioList: ({ children }: RadioListProps) => import("react/jsx-runtime").JSX.Element;
export default RadioList;
