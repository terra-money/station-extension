export interface PageTabsProps {
    tabs: string[];
    activeTab: number;
    onClick: (index: number) => void;
}
declare const PageTabs: ({ tabs, activeTab, onClick }: PageTabsProps) => import("react/jsx-runtime").JSX.Element;
export default PageTabs;
