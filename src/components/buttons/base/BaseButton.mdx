import { Meta, Story, Canvas, Controls, ArgTypes, Description, Source } from '@storybook/blocks';
import Button from './Button.tsx';
import * as ButtonStories from './Button.stories';
import 'scss/storybookoverrides.scss'

<Meta title='Components/Buttons/Base' />

# Base Buttons

These are the Base buttons accessed through `<Button />`. It is used for all actions on the page. It has a few different variations that can be used depending on the action.

## Import

```jsx
import { Button } from '@terra-station/components/buttons';
```

## Props
##### Used as the base for most buttons.
* **`color`** - <span style={{ color: "var(--token-error-500)", fontWeight: "bold", fontSize: "14px" }}>*required*</span> - Sets the color of the button.
  * Options are `primary`, `secondary`, `destructive`, `dashed`, `white-filled`, `outline`.
* **`label`** - <span style={{ color: "var(--token-error-500)", fontWeight: "bold", fontSize: "14px" }}>*required*</span> - Sets the label of the button.
* **`loading`** - Sets the button to a loading state (replaces icon while loading).
* **`block`** - Sets the button to take up the full width of its container.
* **`disabled`** - (included in `ButtonHTMLAttributes`) Sets the button to a disabled state.

# Stories
## Playground
<div className='story_with_source_below'>
  <Canvas of={ButtonStories.Playground} />
  <Controls of={ButtonStories.Playground}/>
</div>

## Primary
The `Primary` button is used for main actions on a page. The label will typically be 'Submit'.
<div className='story_with_source_below'>
  <Canvas of={ButtonStories.Primary} />
  <Source code={`
    <Button color='primary' label='Primary' />
  `} />
</div>

## Primary With Icon
This is the Primary button with an icon passed in.
<div className='story_with_source_below'>
  <Canvas of={ButtonStories.PrimaryWithIcon} sourceState="none" />
  <Source code={`
    <Button
  color='primary'
  label='Primary'
  icon={<SmallCircleCheck fill='var(--token-light-white)' />}
/>
  `} />
</div>

## Secondary
The `Secondary` button is used for secondary actions on a page. The label might be 'Back' or 'Cancel'.
<div className='story_with_source_below'>
  <Canvas of={ButtonStories.Secondary} />
  <Source code={`
    <Button color='secondary' label='Secondary' />
  `} />
</div>

## Destructive
The `Destructive` button is used for destructive purposes such as Deleting accounts.
<div className='story_with_source_below'>
  <Canvas of={ButtonStories.Destructive} />
  <Source code={`
    <Button color='destructive' label='Button' />
  `} />
</div>

## Dashed
The `Dashed` button is used for actions that are not as important as the `Primary` or `Secondary` buttons.
<div className='story_with_source_below'>
  <Canvas of={ButtonStories.Dashed} />
  <Source code={`
    <Button color='dashed' label='Button' />
  `} />
</div>

## White Filled
The `White Filled` button is used in the Welcome flow.
<div className='story_with_source_below'>
  <Canvas of={ButtonStories.WhiteFilled} />
  <Source code={`
    <Button color='white-filled' label='Button' />
  `} />
</div>

## Outline
The `Outline` button is used in the Welcome flow.
<div className='story_with_source_below'>
  <Canvas of={ButtonStories.Outline} />
  <Source code={`
    <Button color='outline' label='Button' />
  `} />
</div>

## Block
Button that takes up the full width of its container.
<div className='story_with_source_below'>
  <Canvas of={ButtonStories.Block} />
  <Source code={`
    <Button block label='Block' />
  `} />
</div>

## Loading
Display Button with an active loading state.
* Requires `loading` prop to be set to `true`.
* Button is disabled when `loading` is active.
<div className='story_with_source_below'>
  <Canvas of={ButtonStories.Loading} />
  <Source code={`
    <Button loading={true} label='Loading' />
  `} />
</div>

## Disabled
Disables the Button.
<div className='story_with_source_below'>
  <Canvas of={ButtonStories.Disabled} />
  <Source code={`
    <Button disabled label='Disabled' />
  `} />
</div>
